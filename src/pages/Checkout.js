import React from 'react'

function Checkout() {
  const styles = {
    position: 'fixed',
    top: '0px',
    left: '0px',
    width: '100vw',
    height: '100vh',
    backgroundColor: 'green',
    zIndex: '-1'
  }
  return <div style={styles}/>
}

export default Checkout